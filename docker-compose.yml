services:
  remupro:
    build: .
    ports:
      - "80:80"
    volumes:
      # Persistir base de datos SQLite
      - remupro-data:/app/data
    restart: unless-stopped
    environment:
      - REMUPRO_ENV=production

volumes:
  remupro-data:
